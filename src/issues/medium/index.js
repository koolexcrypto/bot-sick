import section_M_group1 from './sections/section_M_group1.json' assert { type: 'json' };

let sections = [];
sections = sections.concat(section_M_group1); // add section one. do the same if you want to add new sections/files


export default sections;